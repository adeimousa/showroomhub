import { SuperAdminEntry } from '@/components/super-admin/super-admin-entry'

/**
 * Super admin dashboard alias.
 * URL: showroomhub.com/root-adminstration
 *
 * Same as the root / page on the admin domain — shows login or dashboard.
 */
export default function RootAdministrationPage() {
  return <SuperAdminEntry />
}
